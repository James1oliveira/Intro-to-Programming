var balloons = 100;      // Declare a variable 'balloons' and assign it the value 100
balloons *= 2;           // Multiply the value of 'balloons' by 2 (same as: balloons = balloons * 2)
// balloons is now 200

var balloons = 100;     // Declare a variable 'balloons' and assign it the value 100
balloons /= 4;          // Divide the value of 'balloons' by 4 (same as: balloons = balloons / 4)
// balloons is now 25
